# 连连看 - 消防器材识别

## 一、游戏目的

- 通过经典**连连看**玩法，在消除配对的过程中认识四种灭火器：**二氧化碳灭火器**、**干粉灭火器**、**水基灭火器**、**泡沫灭火器**。
- 每消除一对，会在**游戏容器下方、操作按钮上方**的固定区域展示该灭火器的名称与适用场景小知识（非弹窗），寓教于乐。

---

## 二、游戏界面与布局

### 2.1 整体说明

游戏有三种界面：**开始页**、**进行中**（玩法主界面）、**结束页**。所有界面共用同一**游戏容器**：最大宽度 420px、水平居中、浅色渐变背景，底部预留安全区（如刘海屏/横条区域）。

### 2.2 游戏容器与安全区

| 项目       | 说明 |
| ---------- | ---- |
| **容器宽度** | 最大宽度 **420px**，居中显示。 |
| **背景**     | 浅色渐变（白到浅灰），与主题色系一致。 |
| **安全区**   | 底部预留 `env(safe-area-inset-bottom)`，避免内容被设备底部遮挡。 |
| **进行中底部预留** | 主内容区底部预留约 72px + 安全区高度，为吸底操作栏留出空间，避免被遮挡。 |

### 2.3 开始页布局

- 居中排版，标题「灭火器连连看」+ 简短玩法说明。
- 中部**开始游戏**按钮，样式与主题主色一致（圆角、主色填充）。

### 2.4 游戏进行中页面布局（从上到下）

进行中界面自上而下依次为：

1. **头部栏**  
   - 左侧：玩法提示文案（如「消除相同图案（≤1 折连线）」）。  
   - 右侧：**已消除进度**（如「已消除：0 / 24」），使用成功色强调。

2. **游戏网格**  
   - 圆角矩形区域，浅色底、细边框，内为等分格子。  
   - 列数根据屏幕宽度**响应式**计算（在 48 的因数 4、6 中选取，**最多 6 列**）；行数 = 48 ÷ 列数。  
   - 网格**最大高度不超过视口高度的 2/3**，保证上方头部与下方小知识区、吸底栏均可见。  
   - 格子为**正方形**（aspect-ratio: 1），圆角、有边框与悬停/选中态（选中带聚焦呼吸特效）。

3. **小知识区**  
   - 位于**网格与吸底操作栏之间**，占固定高度，居中对齐。  
   - **有内容时**：展示最近一次消除的灭火器**名称**（加粗、成功色）+ **适用场景小知识**（小号字、次要色）。  
   - **无内容时**：显示占位文案（如「消除一对即可查看小知识」），弱色显示。

4. **吸底操作栏**  
   - **提示**、**重排** 两个按钮，**固定在视口底部**（position: fixed），不随页面滚动。  
   - 栏宽与游戏容器一致（最大 420px 居中），带顶部分割线与背景，底部预留安全区。  
   - 两按钮并排、等风格（主色、圆角），提示在冷却时显示剩余秒数并禁用。

**连线**在网格上方以全屏透明 SVG 层绘制，不参与布局；消除动画、重排抖动均在网格上表现。

### 2.5 结束页布局

- 居中排版，标题「全部消除完成」+ 成绩文案（如「共 24 对灭火器，全部认全啦！」）+ 简短结语。  
- 中部**再玩一局**按钮，样式与开始页一致。

### 2.6 网格与图案数据

| 项目     | 说明                                                                             |
| -------- | -------------------------------------------------------------------------------- |
| **网格** | 列数 4 或 6（响应式），行数 = 48 ÷ 列数；最大高度 ≤ 2/3 视口。                     |
| **图案** | 四种灭火器，每种 **6 对**（12 格），合计 **24 对、48 格**。                       |
| **标识** | 每格**颜色 + 短标签**：CO₂（蓝）、干粉（橙）、水基（绿）、泡沫（紫）。           |

开局时，所有格子随机打乱排列，玩家需要将**相同类型**的灭火器两两配对消除。

### 2.7 视觉与动效简述

- **连线**：全屏透明 SVG 层绘制，主题色细线、拐点小圆角，从起点到终点有绘制过渡动画。  
- **选中**：当前选中的格子有**聚焦呼吸**动效（轻微缩放与光晕循环）。  
- **消除**：配对成功后两格先播**缩放淡出**，再从网格移除。  
- **重排**：点击重排后网格先播**水平抖动**（约 220ms），再打乱顺序。

---

## 三、核心规则：何为「可消除」

### 3.1 基本条件

两个格子可以消除，当且仅当同时满足：

1. **图案相同**：两个格子属于同一种灭火器（即颜色/标签一致）。
2. **可连线**：能用一条**主题色折线**（柔和蓝，非白）把两个格子连起来，且这条折线满足下面的「连线规则」。

### 3.2 连线规则（仅直线或 L 形，无 Z 形）

- 连线只能走**水平**或**竖直**方向，不能走斜线。
- **不允许 Z 形（两折）**：折线最多允许 **1 个拐点**，即最多 **2 段直线**（**直线**或 **L 形**）。
- 折线**除起点和终点外**，只能经过**空格**，不能经过其它未消除的格子。
- 连线使用**主题色细线**（如柔和蓝），拐点处为 90° 直角绘制，从起点到终点有**绘制过渡动画**。

因此，两个格子之间只允许以下两种合法连线：

| 类型     | 拐点数 | 说明                                             |
| -------- | ------ | ------------------------------------------------ |
| **直线** | 0      | 两个格子在同一行或同一列，且中间没有其它格子。   |
| **L 形** | 1      | 先横后竖或先竖后横，拐点处到两端之间均为空。     |

**不允许**：拐两次弯的 Z 形或 U 形（3 段直线）。

### 3.3 连线节点（优先满足 90° 路径）

- 连线的**起点**和**终点**在各自格子的**边界**上，且**优先保证整条路径为 90°**（每段严格水平或竖直）：
  - **起点**：在第一个格子的边界上取一点，使「起点 → 下一个路径点（拐点或终点）」为**严格水平**或**严格竖直**。即：该点是格子边与「通过下一路径点的水平线 y=下一路径点.y」或「竖直线 x=下一路径点.x」的交点，并取朝向下一路径点的那一侧。
  - **终点**：在第二个格子的边界上取一点，使「上一个路径点（拐点或起点）→ 终点」为**严格水平**或**严格竖直**。取法同上。
- 这样保证每一段连线都是水平或竖直的，拐点处自然为 90°。

#### 3.3.1 起点/终点位置的计算逻辑（实现规则）

为保证拐点角度为 **90 度**，起点和终点不按「两格最近点」取，而按「与下一段/上一段成直角」取：

1. **路径形状**  
   - **直线（2 点）**：路径为 [起点, 终点]。下一路径点 = 终点，上一路径点 = 起点。  
   - **L 形（3 点）**：路径为 [起点, 拐点, 终点]。起点的「下一路径点」= 拐点；终点的「上一路径点」= 拐点。

2. **起点**  
   - 在**第一个格子**的边界上取点 P，要求：**P 到「下一路径点」的连线为严格水平或严格竖直**。  
   - 若下一路径点的纵坐标 y 落在该格子上下边之间：取该格子**左边或右边**与直线 **y = 下一路径点.y** 的交点（选朝向下一路径点的一侧），得到**水平段**。  
   - 若下一路径点的横坐标 x 落在该格子左右边之间：取该格子**上边或下边**与直线 **x = 下一路径点.x** 的交点（选朝向下一路径点的一侧），得到**竖直段**。

3. **终点**  
   - 在**第二个格子**的边界上取点 Q，要求：**「上一路径点」到 Q 的连线为严格水平或严格竖直**。  
   - 取法同上：用「上一路径点」的 x 或 y 与格子四条边的交点，选在朝向上一路径点一侧的那一个。

4. **拐点**  
   - L 形时，拐点为中间格子的**几何中心**（同一行或同一列上的中间格中心），保证「起点→拐点」「拐点→终点」两段分别为水平、竖直，拐角为 90°。

5. **绘制**  
   - 连线使用直角连接（如 SVG 的 `stroke-linejoin: miter`），不采用圆角，以明确体现 90° 拐角。

### 3.4 不可消除的情况

- 图案不同（不是同一种灭火器）。
- 图案相同，但只能通过 **2 拐（Z 形）** 才能连上，即被规则禁止。
- 图案相同，且所有合法的直线或 L 形连线都会**经过其它格子**，即被挡住。

---

## 四、操作说明

| 操作               | 说明                                                                                                                                                   |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **选择第一个格子** | 点击一个格子，该格子高亮（带聚焦呼吸特效），表示已选中。**消除动画进行中时**（从配对成功到小知识区更新完成），格子不可点击，避免误触。 |
| **选择第二个格子** | 再点击另一个格子。若与第一个**图案相同**且**可连线**（直线或 L 形），则按「消除反馈流程」执行：先显示**主题色细线**从起点到终点的**绘制过渡** → 连线显示一段时间后，两个格子播放**消除动画**（缩放淡出）→ 动画结束后格子从网格移除，**下方小知识区**更新为该灭火器名称与小知识。 |
| **取消选择**       | 再次点击已选中的格子，则取消选中。 |
| **提示**           | 点击「提示」按钮，系统会高亮一对当前可消除的格子；**使用后进入冷却时间**，冷却期间按钮禁用并显示剩余秒数。若当前无解，会提示「当前无解，请点击重排」。提示/重排按钮**吸底**固定。 |
| **重排**           | 点击「重排」按钮，先播放**网格抖动动效**（约 220ms），再将**当前剩余格子**的图案顺序随机打乱，不增加新牌、不改变对数；重排时**当前选中会清空**。若**当前局面无解**，打乱后会提示「当前无解，已重新排列」。**消除动画或抖动进行中时**重排按钮禁用。 |

### 4.1 消除反馈流程（规则）

一次成功配对后的表现顺序为：

1. **画线**：在起点与终点之间绘制主题色折线，折线从起点到终点有绘制过渡动画。
2. **保持连线**：连线保留一段时间（如约 400ms），供玩家看清路径。
3. **格子消除动画**：两个被消除的格子播放缩放淡出动画（如约 320ms）。
4. **提交消除并更新小知识**：动画结束后，格子从网格中移除，**下方小知识区**更新为该灭火器名称与适用场景小知识（非弹窗）。

在整个「画线 → 消除动画 → 小知识区更新」过程中，视为**忙碌状态**：格子区域不可点击，提示/重排按钮按规则禁用或冷却，避免误触与状态错乱。

---

## 五、胜负与流程

- **进行中**：未消除的格子仍显示在网格中，已消除的格子变为空格。
- **胜利条件**：当所有 **24 对**（48 格）全部消除后，游戏过关，进入结算页。
- **无时间限制**：本游戏不设倒计时，可随时使用「提示」或「重排」。
- **再玩一局**：结算页点击「再玩一局」，重新生成随机布局，开始新一局。

---

## 六、四种灭火器简介（游戏中会随消除逐步展示）

| 类型           | 短标签 | 适用场景简要说明                                                                        |
| -------------- | ------ | --------------------------------------------------------------------------------------- |
| 二氧化碳灭火器 | CO₂    | 锂电池灭火适用性：中。适用配电间/精密仪器实验室。带电设备、精密仪器；不导电、不留残渣。 |
| 干粉灭火器     | 干粉   | 锂电池灭火适用性：中。适用电池生产车间/仓库。油类、气体、带电设备；通用常见。           |
| 水基灭火器     | 水基   | 锂电池灭火适用性：良好。适用 cell 化成、容量、静置车间；固体可燃物（A 类）；忌油类、带电。 |
| 泡沫灭火器     | 泡沫   | 锂电池灭火适用性：不适用。适用电解液房、电解液仓库；油类、易燃液体；在表面形成覆盖层隔绝氧气。 |

消除某一对时，会在下方小知识区展示该类型灭火器的名称与详细小知识，便于记忆。

---

## 七、规则小结（可作游戏内简短说明）

1. **目标**：把相同灭火器两两用线连起来消除，全部消完即过关。
2. **连线**：只能横竖走，**最多一折（L 形）**，不允许 Z 形；中间不能经过其它格子；**起止点优先满足 90°**（在格子边界上取点，使每段严格水平或竖直，拐点直角）；主题色细线，从起点到终点有绘制过渡动画。
3. **表现**：先画线并保留一段时间，再两个格子播放消除动画（缩放淡出），然后移除并在下方小知识区展示该灭火器小知识；消除动画进行中格子与重排不可操作，提示有冷却。选中格子有聚焦呼吸特效；提示/重排按钮吸底固定。
4. **操作**：点选两个格子尝试消除；可用「提示」找一对（有冷却）、「重排」先抖动再打乱剩余牌（无解时会提示「当前无解，已重新排列」，重排时清空选中）。

以上即为《灭火器连连看》的完整规则说明。
